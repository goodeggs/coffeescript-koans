{
  "name": "coffeescript-koans",
  "version": "0.0.2",
  "description": "",
  "devDependencies": {
    "jasmine-node": "1.8.x",
    "coffee-script": "*"
  },
  "engine": {
    "node": ">=0.6"
  },
  "scripts": {
    "test": "jasmine-node --coffee --matchall --match '^About.*' --noStack koans"
  }
}
